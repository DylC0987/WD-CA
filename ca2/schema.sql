-- DROP TABLE IF EXISTS users;

-- CREATE TABLE users (
--     user_id TEXT PRIMARY KEY,
--     password TEXT NOT NULL,
--     score INTEGER DEFAULT 0
--     );



-- For leaderboard testing purposes, created accounts manually through SQL rather than using website, except for account "Test"
-- Insert into users (user_id,password, score)
-- VALUES ('Lily','pass', 32);

SELECT * FROM users;

